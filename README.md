# 2D Game Project

<p align="center">
  <img src="img/so_long.gif.zip" alt="Game animation">
</p>

This is a simple 2D game project entirely written in **C**, using the **MiniLibX (mlx)** graphics library.  
It includes basic graphics programming concepts such as **window management**, **event handling**, and **customizable textures**.

## Requirements
- Linux operating system  
- `cc` compiler  
- `make`  

## Setup
Clone the repository, compile the project

```bash
# Clone the repo
git clone git@github.com:Ines-be/so_long.git
cd so_long

# Compile the project
make

# Launch the game
./so_long maps/map.ber